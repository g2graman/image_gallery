---
ip: "192.168.10.10"
memory: 1024
cpus: 2
provider: virtualbox

#authorize: ~/.ssh/id_rsa.pub

#keys:
#    - ~/.ssh/id_rsa

folders:
    - map: ..
      to: /home/vagrant/image_gallery

sites:
    - map: homestead.app
      to: /home/vagrant/image_gallery/public

databases:
    - homestead

variables:
    - key: APP_ENV
      value: local

# blackfire:
#     - id: foo
#       token: bar
#       client-id: foo
#       client-token: bar

 ports:
     - send: 80
       to: 17000
       protocol: udp
